"""
training-shared — constants shared across all training projects.

Install in each project with:
    pip install -e ../shared

Then import with:
    from training_shared.events import EventType
"""
