[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "training-shared"
version = "0.1.0"
description = "Shared constants for the training project event queue."
requires-python = ">=3.9"
